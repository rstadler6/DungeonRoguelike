using System.Collections.Generic;

namespace DungeonRoguelike.Combat;

public class CollisionChecker
{

    public IEnumerable<(Entity, Entity)> GetCollisions(Entity mainEntity, IEnumerable<Entity> entitiesToCheck)
    {
        yield break;
    }
}